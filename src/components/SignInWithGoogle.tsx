// This file is intentionally empty to prevent build errors
// The component is no longer being used but was still referenced in the repository
export default function SignInWithGoogle() {
  return null;
} 